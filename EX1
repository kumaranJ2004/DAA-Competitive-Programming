#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int arr[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int seen[n + 1]; 
    for (int i = 0; i <= n; i++) {
        seen[i] = 0;
    }

    int duplicate = -1;
    for (int i = 0; i < n; i++) {
        if (seen[arr[i]] == 1) {
            duplicate = arr[i];
            break;
        } else {
            seen[arr[i]] = 1;
        }
    }

    printf("%d\n", duplicate);
    return 0;
}
